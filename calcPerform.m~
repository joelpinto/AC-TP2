function [sensi speci] = calcPerform(outSim, Target)
    i = 1;
    Trglength = size(Trg);
    TP = 0; % true positives
    FN = 0; % false negatives
    TN = 0; % true negatives
    FP = 0; % false positives
    
    while i < Trglength(1,1,1)
        if outSim(3,i) == Target(3,i)
           
        else
            
        end
    end
end
